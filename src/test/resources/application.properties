logging.level.app.tandv.services=trace

# Datasource to a local test file
db.url=jdbc:h2:mem:test-db
db.username=sa
db.secret=
db.class-name=org.h2.jdbcx.JdbcDataSource

db.pool.size=1
db.timeout.connection=500
db.timeout.leak=300
db.timeout.conn-validation=250

# Build Props
pom.version=@project.version@
pom.build.timestamp=@timestamp@